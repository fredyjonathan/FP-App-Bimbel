<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalprojectlnt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/circle_bimbel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/circle_bimbel"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectLnT">
        <activity
            android:name=".homepage"
            android:label="@string/title_activity_homepage"></activity>
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>